{
  "name": "harmony-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "setup:db": "pnpm --filter @harmony/database exec prisma db push",
    "db:seed": "pnpm --filter @harmony/database exec prisma db seed"
  },
  "devDependencies": {
    "turbo": "latest",
    "prettier": "latest",
    "typescript": "latest"
  },
  "packageManager": "pnpm@8.15.0"
}
